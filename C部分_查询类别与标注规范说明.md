# C 部分：查询类别与标注规范说明

## 1. 目的

本说明用于规范教育 RAG 项目 C 部分的自然语言查询、角色化评测单元以及相关性与可见性标注，使后续检索、权限一致性、回答生成和审计实验能够直接使用同一套评测数据。

实验指导要求查询评测同时关注：

1. 查询与文档是否语义相关；
2. 相关文档是否对当前角色可见；
3. 当前教学阶段是否允许访问；
4. 系统应当正常回答、部分回答，还是拒绝回答。

---

## 2. 查询类别

查询集合包括四类：

### 2.1 `QConcept`

概念理解类，用于知识点解释、原理说明、概念比较以及课程内容之间的关联。

### 2.2 `Qlookup`

资源定位类，用于查找课程资料、讲义、课件、实验说明、课程安排或治理记录。

### 2.3 `QAssess`

评估支持类，用于评分标准、作业要求、参考答案、评分说明和助教反馈依据等场景。

### 2.4 `Qmanage`

教学治理类，用于课程归档、质量分析、考试管理、合规记录和教学管理说明等场景。

查询采用完整、清晰的自然语言，并与知识库中的实际资源对应。

---

## 3. 查询 ID 规范

本项目沿用原始查询文件中的稳定 ID：

- `QC001`、`QC002`……对应 `QConcept`；
- `QL001`、`QL002`……对应 `Qlookup`；
- `QA001`、`QA002`……对应 `QAssess`；
- `QM001`、`QM002`……对应 `Qmanage`。

每个 `query_id` 唯一标识一条完整角色化查询记录。即使两条记录的自然语言问题相同，只要角色或教学阶段不同，也应保留各自原始 `query_id`。

---

## 4. 角色与教学阶段

### 4.1 角色

- `student`：学生；
- `ta`：助教；
- `instructor`：任课教师；
- `coordinator`：课程负责人；
- `admin`：管理者。

### 4.2 教学阶段

```text
pre_class
< in_class
< assignment
< pre_exam
< current_exam
< post_exam
< course_archive
```

文档的 `access_policy.role_stage_policy` 表示各角色最早可访问该文档的阶段。

判定规则：

1. 当前角色不在文档策略中：角色受限；
2. 当前角色存在，但当前阶段早于开放阶段：阶段受限；
3. 当前角色存在，且当前阶段达到或晚于开放阶段：文档可见。

---

## 5. 查询记录字段

每条查询记录应保留以下核心字段：

```json
{
  "query_id": "QL001",
  "query_category": "Qlookup",
  "query": "自然语言教育问题",
  "role": "student",
  "stage": "pre_class",
  "rationale": "标注理由",
  "relevant_documents": [],
  "visible_documents": [],
  "hidden_documents": {
    "role_restricted": [],
    "stage_restricted": []
  },
  "distractor_documents": [],
  "answer_policy": "partial"
}
```

字段含义：

- `query_id`：查询主键；
- `query_category`：查询类别；
- `query`：自然语言查询文本；
- `role`：当前用户角色；
- `stage`：当前教学阶段；
- `rationale`：相关性、可见性和回答策略解释；
- `relevant_documents`：全部语义相关文档；
- `visible_documents`：当前角色和阶段可以访问的相关文档；
- `hidden_documents.role_restricted`：因角色限制不可见的相关文档；
- `hidden_documents.stage_restricted`：因阶段限制不可见的相关文档；
- `distractor_documents`：无关普通负样本文档；
- `answer_policy`：系统应采取的回答方式。

---

## 6. 相关性与可见性标注

### 6.1 相关文档

`relevant_documents` 包含所有能够直接支持当前查询的文档，不考虑当前角色是否具有权限。

### 6.2 相关且可见文档

`visible_documents` 同时满足：

1. 与查询语义相关；
2. 当前角色具有访问权限；
3. 当前教学阶段已经达到开放条件。

该集合即角色感知正样本集合。

### 6.3 相关但不可见文档

`hidden_documents` 分为：

- `role_restricted`：当前角色未被授权；
- `stage_restricted`：当前角色原则上可访问，但当前阶段尚未开放。

### 6.4 无关文档

`distractor_documents` 为与查询无关的普通负样本，用于测试系统是否错误返回不相关内容。

---

## 7. 集合一致性要求

每条记录必须满足：

1. `visible_documents` 属于 `relevant_documents`；
2. 两类隐藏文档均属于 `relevant_documents`；
3. 可见文档与隐藏文档不得重叠；
4. 每个相关文档必须被划分为可见或隐藏；
5. `distractor_documents` 不得与相关文档重叠；
6. 所有文档 ID 必须真实存在于四类 D 数据文件中。

即：

```text
visible_documents
∪ hidden_documents.role_restricted
∪ hidden_documents.stage_restricted
= relevant_documents
```

---

## 8. 回答策略

### `answer`

全部相关文档均可见，系统可以正常回答。

### `partial`

至少一份相关文档可见，同时至少一份相关文档受限，系统只能依据可见证据进行部分回答。

### `deny` / `reject`

存在相关文档，但当前角色和阶段看不到任何相关文档，系统应拒绝回答。


---

## 9. 评测场景要求

评测集应尽量覆盖：

1. 同一问题在不同角色下可访问结果不同；
2. 同一问题在不同教学阶段下可访问结果可能不同；
3. 文档语义相关，但策略不允许当前角色访问；
4. 全部相关证据可见时正常回答；
5. 部分相关证据可见时部分回答；
6. 没有任何可见相关证据时拒答。

本次提交严格使用用户确认的最终查询数据，不自动增加未经批准的新查询。

---

## 10. 标注流程

1. 确定查询类别；
2. 标注全部语义相关文档；
3. 根据角色判断角色限制；
4. 根据阶段判断阶段限制；
5. 得到可见相关文档；
6. 选择普通负样本文档；
7. 确定回答策略；
8. 编写 `rationale`；
9. 一致性检查。

---

## 11. 标注粒度

当前 C 部分采用文档级标注。实验指导允许先完成文档级标注，后续回答生成实验需要更细粒度支持时，再针对高质量样本补充 chunk 级标注。

---

## 12. 最终目标

后续实验应能够直接依据上述文件判断：

1. 系统是否找到了语义正确的内容；
2. 系统是否找到了当前角色和阶段应该看到的内容；
3. 在只能部分回答时是否只使用了可见证据；
4. 在不能安全回答时是否进行了合适处理。
